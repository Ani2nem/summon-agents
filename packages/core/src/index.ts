// @summon-agents/core - public surface.
// Real modules (ports, run, worktree, triage, decompose, hotspots, dispatch,
// merge, validate, pr) are added in subsequent tasks and re-exported here.

export const VERSION = "0.0.0";
